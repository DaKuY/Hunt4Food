# Optional: inject at build time for Google star ratings on GitHub Pages.
# Restrict this key to HTTP referrers (dakuy.github.io) in Google Cloud Console.
# You can also add a key in the app Settings page (stored in your browser only).
VITE_GOOGLE_PLACES_API_KEY=
